# 更新记录

## 1.8.9

- 修复双方均为 1.8.8 时，会话包仍可能因 Chromium 独占 `WebStorage/QuotaManager` 而导入失败的问题。
- 导入流程改为先在同盘暂存目录完成解密与存储恢复，再原子移动到尚未启动的目标分区，最后创建 Chromium 会话并导入 Cookie。
- 会话包格式与加密算法继续保持 v1，1.8.8 已导出的会话包可直接导入，无需重新导出。
- 增加包含真实 `WebStorage/QuotaManager` 文件结构、导入后再启动目标分区的打包回归测试。

## 1.8.8

- ChatGPT 用量展示插件更新至 1.1.0，读取官方用量响应中的银行重置（`gpt-reserve`）可用总数。
- 有重置卡时把重置标签合并显示为“X天重置+N”；没有重置卡或接口未返回该字段时保持原标签不变。
- 插件徽标格式增加可选附加字段与正数条件，补充蛇形/驼峰接口兼容及零数量回归测试。

## 1.8.7

- 修复 Windows 更新器在主程序退出后无法继续替换文件、误报“更新器提前退出（0）”的问题。
- 更新工作进程与进度窗口解耦，使用隐藏的独立进程边界；更新窗口只负责展示进度，不再决定更新是否启动。
- 加强便携目录、`data`、更新暂存目录和负载目录校验，失败时记录完整路径诊断并保留原程序与 data。
- 增加更新接管、父进程退出后继续执行、带空格安装路径和更新窗口监控回归测试。

## 1.8.6

- 修复同会话标签页恢复完成后过一会再次刷新、输入内容丢失的问题：性能维护不再删除或重排任何已创建的原生 WebView，Vue/Electron 不会因内存检查重新挂载 Chromium guest。
- 后台标签改为保持 guest 挂载、仅隐藏并关闭交互，性能档位只调整后台帧率、节流和视觉效果；只有用户关闭标签/会话时才释放页面进程。
- 页面进程真实退出时不再自动循环重载，标签会保留并提示，用户点击标签或刷新后才显式恢复；补充 Guest ID、导航计数、DOM 顺序、页面脚本状态和表单输入保留回归测试。

## 1.8.5

- 修复可视化更新器在 WPF/XAML 初始化前提前退出导致的“更新器提前退出（0）”；更新工作进程和接管确认现在先于界面初始化启动，界面失败时仍可安全完成更新。
- 增加更新小窗接管时序回归测试，确认进度窗口异常不会阻断程序替换和启动校验。

## 1.8.4

- 修复同一会话多标签恢复后被性能回收器移除并重新导航的问题；当前会话的已打开标签不再被回收，后台会话仍按性能档位和内存压力管理。
- 增加同会话双标签在严重内存压力下的 Guest ID、导航次数和 WebView 保留回归测试，覆盖普通网页而非特定站点。

## 1.8.3

- 移除会话卡片悬停预连接与 WebView 预加载，鼠标经过会话列表不再创建 Chromium 渲染进程。
- 修复新标签恢复完成时提前裁剪同会话其他页面所导致的重复加载；恢复遮罩改为页面真正停止加载后再撤下。
- 收紧五档常驻 WebView 上限，加入基于实际进程工作集与单页成本的动态内存预算，并降低后台页面帧率。
- 更新下载、解压和临时目录清理统一绕过 Electron 的 ASAR 虚拟文件系统，修复 `app.asar` 被误当作目录时的 `EBUSY` 与无效包错误。
- 增加外部更新器接管确认与启动残留自愈；新版健康检查成功后的临时文件锁定只延后清理，不再回滚已成功安装的版本。
- “重启更新”改为独立的 Windows 原生进度小窗，实时展示接管、备份、替换、健康检查与清理状态，失败时保留可读原因。

## 1.8.2

- 修复备注标签位于首位时，普通网页标签无法拖入首位的问题；不同宽度标签改用边缘跨越相邻中线的实时排序逻辑。
- 优化会话卡片为标题操作行与状态信息行，彩色标签统一单行排列，创建时间改为“x天前”。
- 会话更多菜单增加浏览器区域上方的透明点击拦截层，点击菜单外任意位置即可关闭。
- 修复插件设置页图标被标题辅助文字样式覆盖，确保与插件中心的图标、颜色和尺寸一致。
- ChatGPT 用量展示插件更新至 1.0.1：剩余额度按四档着色，新周期尚未产生用量时不显示重置时间。

## 1.8.1

- 修复自动更新解压 `resources/app.asar` 时被 Electron 文件系统补丁误判为无效包的问题。
- 新增真实 Electron 环境的 ASAR 更新包解压回归测试，并加入 Windows 构建与正式发布流程。

## 1.8.0

- 新增受限的声明式 JSON 插件引擎，统一管理生命周期钩子、权限、定时、独立配置、会话请求与卡片徽标。
- 新增插件中心、仓库安装与更新、SHA-256 校验、本地 JSON 导入及保留配置卸载选项。
- 首个仓库插件为 ChatGPT 用量展示，可在不创建 WebView 的情况下依次更新非免费账号用量。
- 移除会话“可用时间”字段，保留自动回收生命周期管理。
- 修复恢复已保留页面时可能发生的重复状态同步与切换卡顿，并加入同一 WebView 不重复导航的回归测试。
- 插件目录联网刷新移出启动关键路径，缩短冷启动等待时间。
- 低配档悬停改为轻量预连接；内存紧张时只临时释放后台页面，不改变用户固定的性能档位。
- 合并网页导航事件的异步状态同步，减少大型会话列表的深拷贝、IPC 与插件无效调度。

## 1.7.1

- 项目原创部分改用 PolyForm Noncommercial 1.0.0：个人及非商业使用免费，商业使用需取得作者书面授权。
- 新增第三方许可声明，Electron、Chromium、Vue 等组件继续遵循各自许可证。
- README 徽章改为仓库内本地资源，并使用 StarBrowser 正式品牌图标重做 Banner。

## 1.7.0

- 新增启动异步检查、设置页手动检查与忽略指定版本。
- 新增带下载速度和进度的现代化更新浮窗。
- 下载完成后再提示重启，更新过程保护 `data` 并提供程序与配置回滚。
- 新增更新兼容性清单，为状态、浏览器存储和会话包算法迁移预留版本入口。
- 修复鼠标点击按钮后焦点底色不恢复的问题，同时保留键盘焦点提示。
- 统一修复菜单、下拉框、日期面板和确认气泡的窗口边界。
